/* 
NOTE: Removed below command as not possible to reintroduce unique contraint after duplicate codes have been added into flagentries. 
ALTER TABLE flagentries ADD CONSTRAINT flagentries_code_key UNIQUE (code);
*/
